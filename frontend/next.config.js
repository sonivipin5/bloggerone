/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  trailingSlash: true,
  swcMinify: true,
  images: {
    domains: ["res.cloudinary.com"],
  },
}

module.exports = nextConfig
